# Visual Evidence
## Frame Inventory

- `lecture_13_figure_01.png`: studio opening frame with the board at left showing `3,2,1` above a large bracketed `Framework` label; this screenshot should remain in the final notes as the opening visual cue for the method.
- `lecture_13_figure_03.png`: workshop stage frame with two flipcharts, the left showing the `3,2,1` scaffold and the right showing a separate `#Framework` board with `S1` through `S4`; this screenshot should remain in the final notes as the strongest evidence for the lecture’s working board layout.
- `lecture_13_figure_04.png`: inserted board shot showing `Visibility` above a numbered `3,2,1` framework with connector lines and a partially obscuring promo banner; this screenshot should remain in the final notes because it locates `3,2,1` inside a larger framework hierarchy, though it is weaker for exact transcription than `lecture_13_figure_03.png`.

## Equation Extraction

- [visible] `\left(3,2,1\right)`  
  Clearly visible in `lecture_13_figure_01.png`, and visible again in the board material of `lecture_13_figure_03.png` and `lecture_13_figure_04.png`.

- [visible] `\left[\text{Framework}\right]`  
  Clearly visible in `lecture_13_figure_01.png`.

- [visible] `3\ \text{steps},\ 2\ \text{types},\ 1\ \text{thing}`  
  Clearly legible on the left flipchart in `lecture_13_figure_03.png`; present again in `lecture_13_figure_04.png`, although the bottom line is partly obscured by the yellow banner.

- [partially visible] `\#\text{Framework}`  
  Visible as the header of the right flipchart in `lecture_13_figure_03.png`.

- [partially visible] `S_1,\ S_2,\ S_3,\ S_4`  
  Visible as row labels on the right flipchart in `lecture_13_figure_03.png`.

- [partially visible] `\text{Visibility}`  
  Visible as the top heading in `lecture_13_figure_04.png`.

- [partially visible] `2)\ \left(3,2,1\right)`  
  Visible in `lecture_13_figure_04.png`.

- [partially visible] `\text{Business}`  
  The orange word at the right side of `lecture_13_figure_04.png` appears to read this, but the handwriting is not fully secure.

- [standard completion] `S_1 \to S_2 \to S_3 \to S_4`  
  Conservative completion of the right-board staged progression in `lecture_13_figure_03.png`, justified by the visible row ordering and downward directional cue.

- [standard completion] `\{3\ \text{steps},\ 2\ \text{types},\ 1\ \text{thing}\}`  
  Editorial compression of the visible scaffold across `lecture_13_figure_03.png` and `lecture_13_figure_04.png`.

## Diagram Extraction

- `lecture_13_figure_01.png` is not a worked diagram but a title-layout diagram: `3,2,1` sits above `[Framework]`, visually introducing the lecture’s central object. This should be preserved as a screenshot and, if redrawn at all, only as a very small companion title-style TikZ panel.
- `lecture_13_figure_03.png` contains the clearest diagrammatic content. The left flipchart is a linear scaffold with a top heading and three descending slots: `3 steps`, `2 types`, `1 thing`. The right flipchart is a separate staged model: a top heading `#Framework`, then four rows labeled `S1` through `S4`, with longer handwritten text and a red downward arrow between middle stages. This is the best candidate for a screenshot-plus-TikZ pairing.
- `lecture_13_figure_04.png` shows the `3,2,1` scaffold embedded inside a larger board hierarchy. `Visibility` is placed above a numbered `2) 3,2,1`, then the scaffold is listed beneath, with colored connector lines drawn toward a right-side annotation. This should be kept as a screenshot and can also support a simplified TikZ redraw of the board hierarchy.

## Reconstruction Guidance

- Keep `lecture_13_figure_01.png` visible in the final notes as evidence that the lecture opens by naming the central object. Nearby, reconstruct only the minimal visible notation:
  `\left(3,2,1\right)` above `\left[\text{Framework}\right]`.
  Do not inflate this into a larger derivation.
- Keep `lecture_13_figure_03.png` visible and place the cleanest reconstruction work next to it. Two companion reconstructions are justified:
  - a displayed transcription of `3\ \text{steps},\ 2\ \text{types},\ 1\ \text{thing}`;
  - a small TikZ schematic for the right-board stage structure, preserving the vertical `S_1` to `S_4` order and the visible downward motion cue.
- Keep `lecture_13_figure_04.png` visible as evidence for board organization rather than for exact full transcription. A nearby TikZ redraw should preserve:
  - `\text{Visibility}` at the top;
  - the numbered `2)\ \left(3,2,1\right)` beneath it;
  - the three-line scaffold below;
  - optional connector lines toward a right-side application label.
- When reconstructing from `lecture_13_figure_04.png`, do not reproduce the yellow promotional banner; it is an artifact of the inserted clip, not part of the note-worthy framework.
- Across all three images, the safest note-writing strategy is screenshot-plus-cleaned-notation, not screenshot replacement. The screenshots carry the lecture’s board style and layout; the clean LaTeX should make the structure readable without pretending that the boards were more precise than they are.

## Uncertainties

- In `lecture_13_figure_01.png`, the purple cloud outline around `3,2,1` is cropped at the top edge, so only the label itself is reliable.
- In `lecture_13_figure_03.png`, the long handwritten text beside `S1` through `S4` is not reliably legible from the frame alone. It should not be transcribed verbatim without another confirming source.
- In `lecture_13_figure_03.png`, the red arrow clearly signals direction, but the full graph structure is not explicit; a simple vertical progression is the safest reconstruction.
- In `lecture_13_figure_04.png`, the orange annotation to the right appears to read `Business`, but this is not certain enough to treat as fully visible notation.
- In `lecture_13_figure_04.png`, the yellow banner hides the lower portion of the board, so the bottom of the `3,2,1` scaffold should be treated as only partly visible in that frame.
- The lecture’s visible content is framework notation and board organization rather than formal mathematics. Any compact symbolic notation beyond the board labels should be marked as editorial reconstruction, not blackboard transcription.