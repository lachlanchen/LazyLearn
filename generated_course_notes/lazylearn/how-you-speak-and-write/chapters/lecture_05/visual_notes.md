# Visual Evidence
## Frame Inventory
- `lecture_05_figure_03.png`: A medium shot of the speaker beside a whiteboard headed `5. OUTLINE`, with four handwritten bullet points and two parenthetical sublabels; this screenshot should remain in the final notes because it preserves the lecture’s explicit board layout for Step 5.

## Equation Extraction
- There are no mathematical equations visible in this frame; the extractable content is structured whiteboard notation and labels.
- [visible] `5.\ \text{OUTLINE}`
- [visible] `\text{Fiction vs. Non-fiction}`
- [visible] `(\text{King})`
- [visible] `(\text{Agents/editors})`
- [visible] `\text{The Marathon of the Middle}`
- [visible] `\text{Same structure}`
- [visible] `\text{Outline serves } \underline{\text{you}}`
- [standard completion] If the notes want a cleaned typeset version, render these as a compact displayed list rather than as equations.

## Diagram Extraction
- The board is not a physics-style diagram or derivation; it is a top-down outline.
- The only internal structure that matters visually is the contrast `Fiction vs. Non-fiction`, with the parenthetical notes placed beneath the two sides.
- The remaining items are linear bullets rather than nodes, arrows, or branches:
  - `The Marathon of the Middle`
  - `Same structure`
  - `Outline serves you`
- This figure should be preserved as a screenshot, not redrawn in TikZ.
- A nearby clean transcription may be useful in the notes, but it should be treated as a typeset companion list, not as a reconstructed diagram.

## Reconstruction Guidance
- Keep `lecture_05_figure_03.png` visible in the final chapter as the primary visual evidence for Step 5.
- Reconstruct the board content nearby as a clean list in prose or display form:
  - `5. Outline`
  - `Fiction vs. Non-fiction`
  - `Stephen King` on the fiction side
  - `Agents/editors` on the nonfiction side
  - `The Marathon of the Middle`
  - `Same structure`
  - `Outline serves you`
- Do not introduce equations or formal notation beyond the visible labels; the lecture here is structural, not mathematical.
- Do not replace the screenshot with TikZ. If a cleaner presentation is desired, pair the screenshot with a short textual reconstruction rather than a redraw.
- If cropped in layout, preserve the full whiteboard area and the underlined `you`, since that emphasis is part of the lecturer’s point.

## Uncertainties
- The exact attachment of `(King)` and `(Agents/editors)` to the two sides of `Fiction vs. Non-fiction` is inferred from placement rather than from brackets or arrows.
- `(Agents/editors)` is slightly softer than the other writing because it sits near the right edge of the board.
- The lower-third `5. Construct your outline.` is on-screen overlay text, not whiteboard content, so it should not be transcribed as part of the board.
- No mathematical symbols, indices, or derivation steps are visible, so any stronger formalization in the final notes would be editorial rather than frame-backed.