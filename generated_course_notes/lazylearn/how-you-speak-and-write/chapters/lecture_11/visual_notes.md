# Visual Evidence
## Frame Inventory
- `lecture_11_figure_03.png`: Wide chalkboard view showing a two-block board layout with `SAMPLES` on the left, `TIME & PLACE` on the right, a loop sketch beside `Cycle`, and a long wavy divider; this screenshot should remain in the final notes.
- `lecture_11_figure_04.png`: The lecturer holds a bicycle wheel upright by the hub as a prop, with a partial background formula and stove sketch still on the board; this screenshot should remain in the final notes.

## Equation Extraction
- `lecture_11_figure_03.png`: no genuine equation is visible; the frame is organizational rather than algebraic.
- `lecture_11_figure_04.png`: [partially visible] `\text{Quality} = f(K, P, \ldots)`
- `lecture_11_figure_04.png`: [partially visible] `\text{Quality}`
- `lecture_11_figure_04.png`: [standard completion] `Q = f(K, P, T)`
  - Use this only if the notes need a clean statement of the opening formula and make clear that the full right-hand side is completed from transcript context rather than fully read off the frame.

## Diagram Extraction
- `lecture_11_figure_03.png`: The important visual is the board architecture itself. It should be shown both as the original screenshot and as a clean TikZ-style reconstruction with a left `Samples` block, the four listed heuristics, a loop/cycle icon beside `Cycle`, and a right `Time \& Place` block separated by a long wavy line.
- `lecture_11_figure_03.png`: The loop beside `Cycle` is not a formal state diagram, but it is worth redrawing as a schematic repeated-pass graphic because it materially supports the “go around it again” idea.
- `lecture_11_figure_04.png`: The bicycle wheel demonstration should be preserved primarily as a screenshot, because the physical pose, wheel orientation, and hand placement are the main evidence.
- `lecture_11_figure_04.png`: If the notes later explain the wheel puzzle, add a very small auxiliary schematic of a marked rim segment, the top position of that segment, and the direction of the applied puff/torque; keep the screenshot nearby and treat the schematic as an explanatory redraw, not a board transcription.
- `lecture_11_figure_04.png`: The stove/firebox sketch in the background should not be redrawn for this section unless the earlier props discussion is being reconstructed separately.

## Reconstruction Guidance
- Keep `lecture_11_figure_03.png` visible and use it as evidence for Winston’s blackboard method: headings, spatial separation, list structure, and a hand-drawn graphic all working together on one board.
- Redraw `lecture_11_figure_03.png` more cleanly than literally. Preserve the headings and the four heuristic labels exactly where they are secure, but treat the right-side subitems under `TIME & PLACE` cautiously unless they are rechecked from transcript or neighboring frames.
- Treat the partial `Quality = f(K,P,\ldots)` in `lecture_11_figure_04.png` as background carryover only. If the chapter includes the formula, present it once in clean form elsewhere and do not make the bicycle-wheel figure carry that mathematical load.
- Do not infer a full gyroscopic derivation from `lecture_11_figure_04.png`. Equations such as `\tau = d\mathbf{L}/dt` or precession formulas are not frame-backed here and should not be introduced as if they were visible.
- If the wheel example is formalized, follow the lecture’s own local-piece reasoning instead: mark one small taped rim segment, place it at the top, show the applied puff, and infer the rolling direction piece by piece.
- Keep the screenshot evidence adjacent to any redraw whenever the redraw clarifies rather than replaces the original classroom visual.

## Uncertainties
- In `lecture_11_figure_03.png`, the right-side checklist under `TIME & PLACE` is partly hidden by the lecturer; `11 a.m.` is suggested, but the remaining items are not fully readable from the frame alone.
- The loop graphic beside `Cycle` in `lecture_11_figure_03.png` is clearly a cyclic sketch, but its internal stroke order and any implied arrowing are not fully secure.
- In `lecture_11_figure_04.png`, the background formula is incomplete; `K` and `P` are legible, but the last argument is not fully readable from the image itself.
- The apparent completion to `Q = f(K,P,T)` comes from transcript context, not from a complete visual transcription of the frame.
- The bicycle-wheel frame shows no explicit mathematical notation for torque, angular momentum, or precession; any such notation would be an editorial addition, not extracted content.
- The stove/firebox drawing in `lecture_11_figure_04.png` is residual board content from the earlier props discussion and should not be overread as part of the wheel argument.