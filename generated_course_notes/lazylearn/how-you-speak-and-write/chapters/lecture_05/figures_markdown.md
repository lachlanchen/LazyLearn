# Figure Notes
## Image Inventory
- `lecture_05_figure_03.png`: A medium shot of the speaker standing beside a whiteboard. The whiteboard is the main useful visual element. It shows a large heading, `5. OUTLINE`, followed by four handwritten bullet points: `Fiction vs. Non-fiction`, `The Marathon of the Middle`, `Same structure`, and `Outline serves you`, with `you` underlined. Two parenthetical notes sit beneath the first bullet, one under the fiction side, ` (King)`, and one under the nonfiction side, `(Agents/editors)`. A lower-third overlay at the bottom reads `5. Construct your outline.`

## Blackboard Equations
- [visible] `5.\ \text{OUTLINE}`
- [visible] `\text{Fiction vs. Non-fiction}`
- [visible] `(\text{King})`
- [visible] `(\text{Agents/editors})`
- [visible] `\text{The Marathon of the Middle}`
- [visible] `\text{Same structure}`
- [visible] `\text{Outline serves } \underline{\text{you}}`

There are no mathematical equations in this screenshot; the visible content is structured whiteboard notation and outline text.

## Diagram And Layout Reading
- The board is organized as a top-down outline rather than a diagram.
- The heading `5. OUTLINE` anchors the whole board and signals that this is a named lecture step, not an incidental aside.
- The first bullet has the only real internal structure: `Fiction vs. Non-fiction` is written as a contrast, and the two parenthetical notes below it appear to label the two sides separately, with `King` attached to the fiction side and `Agents/editors` attached to the nonfiction side.
- The remaining three bullets are linear reminders rather than branches or arrows:
  - `The Marathon of the Middle`
  - `Same structure`
  - `Outline serves you`
- The underlining of `you` is rhetorically important. It visually stresses that the outline is a tool for the writer, not a rigid master-plan.
- The lower-third `5. Construct your outline.` duplicates the step framing, but the whiteboard itself carries the more valuable content.

## TeX Reconstruction Plan
- `lecture_05_figure_03.png` should remain visible in the final notes. It is the only accepted frame here that materially preserves board layout and handwritten emphasis.
- The screenshot should be kept primarily as screenshot evidence, not replaced by TikZ. There is no genuine diagram to redraw.
- Nearby in the text, reconstruct the board content as a compact outline or short displayed list rather than as an equation. A faithful reconstruction would include:
  - `5. Outline`
  - `Fiction vs. Non-fiction`
  - parenthetical subnotes for fiction and nonfiction: `Stephen King` and `agents/editors`
  - `The Marathon of the Middle`
  - `Same structure`
  - `Outline serves you`
- No standalone equation is needed.
- No TikZ redraw is needed unless the book design demands a clean companion list; even then, a simple bullet list is preferable to a diagram.
- If cropping is used in layout, preserve the full whiteboard and the underlined `you`. The speaker may remain in frame, but the board should not be cropped tighter than the current right and lower edges.

## Caption Drafts
- `lecture_05_figure_03.png`: Whiteboard outline for Step 5: fiction, nonfiction, the middle, and the role of the outline.

## Uncertainties
- The first bullet is clearly `Fiction vs. Non-fiction`, but the exact visual attachment of the parenthetical notes is inferred from placement rather than from drawn arrows or brackets.
- `(Agents/editors)` is legible, though slightly softer than the other text because of its position near the right side of the board.
- The lower-third `5. Construct your outline.` is on-screen overlay text, not part of the whiteboard itself; it should not be confused with the handwritten board content.
- No mathematical content is present, so any later formalization in the notes would be editorial rather than visually extracted.