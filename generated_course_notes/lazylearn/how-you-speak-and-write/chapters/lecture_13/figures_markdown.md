# Figure Notes
## Image Inventory

- `lecture_13_figure_01.png`: studio talking-head frame with a vertical whiteboard/screen at left. The visible board content is a purple cloud-like outline containing `3,2,1` near the top and a large bracketed `Framework` label below it in teal, with a teal underline. The speaker and microphone dominate the foreground, but the board text is still legible and useful as an opening layout marker.
- `lecture_13_figure_03.png`: stage workshop frame with two flipcharts behind the speaker. The left flipchart clearly shows the `3,2,1` scaffold with three lines underneath: `3 steps`, `2 types`, `1 thing`. The right flipchart is headed `#Framework` and contains a vertical sequence labeled `S1`, `S2`, `S3`, `S4`, with longer handwritten stage labels and a red downward arrow between the middle rows.
- `lecture_13_figure_04.png`: inserted promo-style board shot framed inside a textured border. The board shows the heading `Visibility` at the top, then a numbered line `2) 3,2,1`, then the three-line scaffold `3 steps`, `2 types`, `1 thing`. Pink/orange connector lines run from the scaffold toward a right-side orange annotation that appears to read `Business`. A bright yellow promo banner obscures the lower part of the board.

## Blackboard Equations

- [visible] `\left(3,2,1\right)`  
  Visible in `lecture_13_figure_01.png` and again in the board material of `lecture_13_figure_03.png` and `lecture_13_figure_04.png`.

- [visible] `\left[\text{Framework}\right]`  
  Clearly visible in `lecture_13_figure_01.png`.

- [visible] `3\ \text{steps},\ 2\ \text{types},\ 1\ \text{thing}`  
  Clearly legible on the left flipchart in `lecture_13_figure_03.png`; also visible again in `lecture_13_figure_04.png`, though the lowest line is partly obscured by the promo banner.

- [partially visible] `\#\text{Framework}`  
  Visible as the header on the right flipchart in `lecture_13_figure_03.png`.

- [partially visible] `S_1,\ S_2,\ S_3,\ S_4`  
  Visible as stage markers on the right flipchart in `lecture_13_figure_03.png`.

- [partially visible] `\text{Visibility}`  
  Visible as the top heading in `lecture_13_figure_04.png`.

- [partially visible] `2)\ \left(3,2,1\right)`  
  Visible in `lecture_13_figure_04.png`.

- [partially visible] `\text{Business}`  
  The orange right-side annotation in `lecture_13_figure_04.png` appears to read this, but the handwriting is not fully secure.

- [standard reconstruction] `S_1 \to S_2 \to S_3 \to S_4`  
  Reasonable reconstruction of the staged sequence in `lecture_13_figure_03.png`, supported by the vertical ordering and the visible red downward arrow, but the exact connection pattern is not fully explicit in the image.

- [standard reconstruction] `\{3\ \text{steps},\ 2\ \text{types},\ 1\ \text{thing}\}`  
  Compact editorial summary of the scaffold visible across `lecture_13_figure_03.png` and `lecture_13_figure_04.png`.

## Diagram And Layout Reading

- `lecture_13_figure_01.png` is an opening board-layout frame rather than a worked diagram. Its value is architectural: the lecture’s core object is introduced as a named framework, with `3,2,1` visually stacked above `[Framework]`. The board behaves like a title or label card for the method.
- `lecture_13_figure_03.png` contains the strongest structural evidence. The left board is a clean linear scaffold: top title `3,2,1`, then three descending slots for `3 steps`, `2 types`, `1 thing`. The right board is a second framework with a separate heading and a staged vertical progression `S1` through `S4`. The red downward arrow suggests development or transition through middle stages. The two-board composition matters: one board gives the compact fallback speaking template, the other shows a broader staged model.
- `lecture_13_figure_04.png` shows the `3,2,1` scaffold embedded within a larger board organization. `Visibility` sits above the numbered `2) 3,2,1`, so the board seems to place this framework as one item inside a broader sequence or list of communication frameworks. The pink/orange connector lines leading to the orange note at right suggest an application domain or example target. The lower yellow promo strip blocks part of the board, so the screenshot is more useful for layout and hierarchy than for exact full transcription.

## TeX Reconstruction Plan

- `lecture_13_figure_01.png` should remain visible in the chapter as the opening visual cue for the method. Nearby, reconstruct only the minimal notation
  `\left(3,2,1\right)` and `\left[\text{Framework}\right]`
  as clean displayed text or a very small TikZ title-style panel. No elaborate redraw is needed.
- `lecture_13_figure_03.png` must remain visible. It is the strongest screenshot for both board layout and notation. Pair it with:
  - a clean displayed reconstruction of `3\ \text{steps},\ 2\ \text{types},\ 1\ \text{thing}`;
  - a simple TikZ or structured list for the right-board stage progression, preserving the `S1`–`S4` vertical order and the downward motion cue.
- `lecture_13_figure_04.png` should also remain visible because it shows the `3,2,1` framework nested under `Visibility`, which adds hierarchical context not present in the other frames. Pair it with:
  - a clean displayed reconstruction of the numbered item `2)\ (3,2,1)`;
  - a small TikZ schematic showing `Visibility` above the `3,2,1` scaffold, plus optional connector lines toward a right-side application label.
- The screenshots should stay as evidence; the LaTeX reconstructions should sit nearby as cleaned companions, not replacements.
- The yellow promotional banner in `lecture_13_figure_04.png` should not be reproduced in TikZ or equation form; it is part of the source frame, not part of the note-worthy framework.

## Caption Drafts

- `lecture_13_figure_01.png`: Opening `3,2,1` framework board.
- `lecture_13_figure_03.png`: Two-board view of the `3,2,1` scaffold and staged framework.
- `lecture_13_figure_04.png`: `Visibility` board with the numbered `3,2,1` framework.

## Uncertainties

- In `lecture_13_figure_01.png`, the purple cloud outline is cropped at the top edge; `3,2,1` is visible, but the full surrounding shape is not.
- In `lecture_13_figure_03.png`, the longer handwritten labels beside `S1`–`S4` are not fully secure from the image alone. They resemble stage names of a competence or communication progression, but they should be transcribed cautiously unless confirmed from another frame or the spoken lecture.
- In `lecture_13_figure_03.png`, the exact role of the red arrow is clear as a directional cue but not fully clear as a complete graph structure; a conservative vertical progression is safest.
- In `lecture_13_figure_04.png`, the orange right-side annotation appears to read `Business`, but the handwriting is not perfectly sharp.
- In `lecture_13_figure_04.png`, the yellow promo banner obscures the lower board, so the full `1 thing` line and any lower annotations should not be treated as fully visible evidence.
- Across these images, the central object is rhetorical framework notation rather than formal mathematics; any compact LaTeX beyond the visible labels should be signposted as editorial reconstruction rather than blackboard transcription.